spring:
 config:
  activate:
   on-profile: log
 jpa:
  open-in-view: false
  properties:
    hibernate:
     show_sql: true 
     format_sql: true
     highlight_sql: true
     use_sql_comments: true
logging:
 level:
  org:
   hibernate:
    SQL: debug
    orm:
     jdbc:
      bind: trace
    
  
 
 